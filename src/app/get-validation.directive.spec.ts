import { GetValidationDirective } from './get-validation.directive';

describe('GetValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new GetValidationDirective();
    expect(directive).toBeTruthy();
  });
});
